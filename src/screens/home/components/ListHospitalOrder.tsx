import {StyleSheet, Text, View} from 'react-native';
import React, {memo} from 'react';

const ListHospitalOrder = () => {
  return (
    <View>
      <Text>ListHospitalOrder</Text>
    </View>
  );
};

export default memo(ListHospitalOrder);

const styles = StyleSheet.create({});
