import {StyleSheet, Text, View} from 'react-native';
import React, {memo} from 'react';

const ContentSpecialty = () => {
  return (
    <View>
      <Text>ContentSpecialty</Text>
    </View>
  );
};

export default memo(ContentSpecialty);

const styles = StyleSheet.create({});
